apiVersion: 1
datasources:
  - name: imagunet-glpi-datasource
    type: imagunet-glpi-datasource
    access: proxy
    isDefault: true
    orgId: 1
    version: 1
    editable: true
    jsonData:
      glpiUrl: 'http://glpi-backend:8765'
      defaultEntityId: ''
      verifySSL: false
      backendUrl: 'http://glpi-backend:8765'
    secureJsonData:
      userToken: 'test-token'
